package com.smartcampus.enums;

/** Lifecycle states of a maintenance/incident ticket. Member 3 responsibility. */
public enum TicketStatus {
    OPEN,
    IN_PROGRESS,
    RESOLVED,
    CLOSED,
    REJECTED
}
